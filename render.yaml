services:
  - type: web
    name: israel-salary-calculator
    runtime: static
    buildCommand: echo "Static site ready"
    staticPublishPath: ./public
    headers:
      - path: /*
        name: Cache-Control
        value: public, max-age=3600
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
